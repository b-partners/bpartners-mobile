name: CI

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  install-and-test:
      runs-on: ubuntu-latest
      steps: 
        - uses: actions/checkout@v2
        - run: npm install --force
        - run: npm test

